子模块分发器

一，概述：

	此工具旨在提供一个拷贝代码的快捷方法。程序使用元数据替换的方式将模板中带@的字段进行替换后生成目标文件。

二，用法：

1，此工具用于将一个模板文件中的字段序列分别分发成不同的文件。

2，此工具集成批处理的功能，点击app.bat即可自动运行。

3，此工具的模板文件放在resource目录下，生成文件放在resource_output目录。

4，此工具提供开源版本，编辑修改完成后需要运行pack_all.bat生成新的可执行jar。